properties:
  template:
    containers:
    - image: bomtoolregistry.azurecr.io/bom-tool:latest
      name: bom-tool-api
      command:
      - uvicorn
      args:
      - main:app
      - --host
      - "0.0.0.0"
      - --port
      - "8000"
      - --workers
      - "2"
