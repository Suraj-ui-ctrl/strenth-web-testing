export default function AIScoringViewer(_props: {
  onClose: () => void
  onPlaceOrder: (_cmName: string, price: number) => void
}) {
  return <div className="p-8 text-center text-gray-400">AI Scoring — coming soon</div>
}
